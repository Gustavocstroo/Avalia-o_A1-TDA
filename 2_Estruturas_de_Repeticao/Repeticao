# Programa que exibe números pares de 1 a 100
# usando duas estruturas de repetição: for e while.

print("Números pares de 1 a 100 usando FOR:")

# Laço FOR percorre de 1 a 100
for num in range(1, 101):
    # Verifica se o número é par
    if num % 2 == 0:
        print(num)

print("\nNúmeros pares de 1 a 100 usando WHILE:")

# Inicializa a variável para o while
num = 1

# Enquanto num for menor ou igual a 100
while num <= 100:
    # Verifica se é par
    if num % 2 == 0:
        print(num)
    # Incrementa o contador
    num += 1
